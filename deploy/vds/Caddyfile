{$SITE_HOST:localhost} {
    encode zstd gzip

    handle_path /tiles/* {
        reverse_proxy tiles:3000
    }

    handle /api/* {
        reverse_proxy api:8000
    }

    handle /health {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy web:80
    }
}
